# Railway 배포 설정
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# 헬스체크 경로 (/health 엔드포인트는 백엔드에서 구현 필요)
healthcheckPath = "/health"
healthcheckTimeout = 300

# 재시작 정책 (실패 시에만 재시작, 최대 5번)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
